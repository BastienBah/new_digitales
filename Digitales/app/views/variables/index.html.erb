<div id="var">

  <div class="title">
    <h1 class="section-title">Dimensions</h1>
    <p class="paragraph">Cherchez un terme particulier, une dimensions ou mëme seulement une lettre. Si ce que vous cherchez n’est pas encore féférencer ici, soumettez nous votre mot et votre indéfinitions.</p>
  </div>

  <div class="container-var">
    <ul>
      <% @variables.each do |variable| %>
          <li>
            <em><%= variable.name %></em>
            <%= link_to "portraits", "/portraits?variable="+variable.id.to_s %>
            <%= link_to "indefinitions", "/indefinitions?variable="+variable.id.to_s %>
          </li>
      <% end %>
    </ul>
  </div>

</div>
